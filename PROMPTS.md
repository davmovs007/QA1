# Журнал промптов (Prompt Engineering Log)

В данном файле задокументированы абсолютно все запросы (промпты), которые отправлялись искусственному интеллекту на каждом этапе разработки проекта «Менеджер задач» (Task Manager). 

Файл демонстрирует навыки **Prompt Engineering**: декомпозицию задач, задание ролей, формулирование архитектурных и технологических ограничений (чистая Java без систем сборки, Swing, java.io), валидацию и пошаговую модернизацию кода.

---

## Этап 1: Создание предметной области и бизнес-логики (Model & Service)
**Автор промпта:** Студент Б

**Цель:** Создание структуры сущностей предметной области, перечислений с локализацией и менеджера управления задачами с поддержкой сериализации и файлового ввода-вывода (CSV/TXT).

**Промпт:**
> **Роль:** Ты Senior Java Backend Developer.
> 
> **Стек и ограничения:** 
> - Только чистая Java (стандартные библиотеки). 
> - Никаких систем сборки (Maven, Gradle).
> - Без использования сторонних библиотек и фреймворков.
> - Написание unit-тестов не требуется, фокус на архитектуре.
> 
> **Задание:**
> Напиши базовую архитектуру для приложения "Менеджер Задач" (To-Do List). 
> 1. Создай пакет `model`. В нем сделай класс `Task` (id, title, description, createdAt, dueDate). Также добавь два enum: `Priority` (LOW, MEDIUM, HIGH) и `TaskStatus` (TODO, IN_PROGRESS, DONE). В enum добавь русские названия через метод `getDisplayName()`.
> 2. Создай пакет `service`. В нем напиши класс `TaskManager`, который будет инкапсулировать список задач (`List<Task>`) и отвечать за CRUD-операции над ними.
> 3. Добавь метод для сохранения и загрузки списка задач в файл в формате CSV/txt с использованием исключительно базовых классов пакета `java.io` (например, `FileWriter`, `BufferedReader`).

**Результат этапа:** Созданы пакеты `model` (`Task`, `Priority`, `TaskStatus`) и `service` (`TaskManager`, `FileManager`).

---

## Этап 2: Создание базового графического интерфейса (Swing UI)
**Автор промпта:** Студент А

**Цель:** Реализация GUI на Java Swing: главное окно с таблицей задач, модальное диалоговое окно для добавления и редактирования задач, пользовательская модель таблицы `TaskTableModel`.

**Промпт:**
> **Роль:** Ты Senior Java Developer. 
> 
> **Стек и ограничения:** 
> - Только чистая Java (стандартные библиотеки). 
> - Использование Maven, Gradle или других систем сборки СТРОГО запрещено.
> - Сторонние библиотеки не используются. 
> - Для графического интерфейса применять только **Java Swing**.
> 
> **Контекст задачи:** 
> Мой напарник уже разработал бизнес-логику для Менеджера Задач. 
> В пакете `model` у нас есть классы: `Task`, `Priority` и `TaskStatus`. 
> В пакете `service` у нас есть готовый `TaskManager`, реализующий CRUD-операции и работу с `java.io`.
> 
> **Задание:**
> Напиши визуальную часть приложения (пакет `ui`) на Java Swing. 
> Мне нужны следующие составляющие:
> 1. `MainFrame` — Главное окно, содержащее `JTable` для отображения всех задач и панель инструментов (кнопки "Добавить", "Редактировать", "Удалить", "Сохранить", "Загрузить").
> 2. `TaskTableModel` — Собственная модель таблицы (наследуется от `AbstractTableModel`), которая связывает `List<Task>` со столбцами таблицы в Swing.
> 3. `TaskDialog` — Модальное диалоговое окно (JDialog) для ввода данных при создании или редактировании задачи (содержит JTextField, JComboBox и т.д.).
> 4. `Main` — Главный класс приложения с методом `main`, вызывающий `MainFrame` через `SwingUtilities.invokeLater()`.

**Результат этапа:** Реализованы `ui.MainFrame`, `ui.TaskTableModel`, `ui.TaskDialog`, `Main`.

---

## Этап 3: Добавление поиска и расширенной фильтрации
**Автор промпта:** Студент А

**Цель:** Добавление панели фильтрации задач по текстовому запросу, статусу и приоритету с использованием Java Stream API.

**Промпт:**
> **Роль:** Ты Senior Java Developer. 
> 
> **Стек:** Только Java 8+ (стандартная библиотека) и Java Swing.
> 
> **Контекст:** 
> В приложении (Task Manager) написана визуальная часть (окно `ui.MainFrame`). Однако в тулбаре мало кнопок и нет сложного поиска. Мне нужно добавить продвинутую систему фильтрации.
> В классе `TaskManager` есть метод `getAllTasks()`, возвращающий `List<Task>`, а `TaskStatus` и `Priority` — это `enum`.
> 
> **Задание:**
> Обнови интерфейс `MainFrame` и логику отображения данных в `JTable`, реализовав комбинацию поиска и фильтров.
> 1. Раздели верхнюю часть окна (NORTH) на две подпанели (`GridLayout(2, 1)`): панель действий (кнопки CRUD) и панель фильтров.
> 2. В панель фильтров добавь:
>    - Текстовое поле для поиска (по названию или описанию).
>    - `JComboBox` для `TaskStatus` (сгенерируй из `TaskStatus.values()`).
>    - `JComboBox` для `Priority` (сгенерируй из `Priority.values()`).
>    - Кнопки "Применить фильтры" и "Сбросить".
> 3. Напиши метод `applyFilters()`, который будет брать весь список задач через `taskManager.getAllTasks()` и через `Stream API` применять все условия, перерисовывая `tableModel`. 
> 4. Не меняй классы `model` и `service` (инкапсулируем логику фильтрации представления пока что в `MainFrame`). 

**Результат этапа:** В `MainFrame` интегрирована двухрядная панель управления и фильтрации с динамическим обновлением `JTable`.

---

## Этап 4: Рефакторинг и внедрение архитектурного паттерна MVC
**Автор промпта:** Студент А

**Цель:** Разделение ответственности (SRP), выделение контроллера `TaskController` и настройка Dependency Injection.

**Промпт:**
> **Роль:** Ты Lead Java Engineer и эксперт по паттернам проектирования.
> 
> **Стек:** Только Java 8+ (стандартная библиотека) и Swing.
> 
> **Контекст задачи:** 
> Сейчас в проекте класс `ui.MainFrame` (View) нарушает принцип единой ответственности (SRP). Он сам обращается напрямую к моделям, сам занимается логикой фильтрации (Stream API) и сам проводит автозагрузку/автосохранение.
> 
> **Задание:**
> Отрефактори код, переведя архитектуру полностью на паттерн **MVC (Model-View-Controller)**. 
> 1. Создай новый пакет `controller` и в нем класс `TaskController`.
> 2. Вынеси всю бизнес-логику из `MainFrame` в `TaskController`: логику добавления/удаления задач, работу с файлами (вызовы сохранения) и всю сложную логику применения фильтров `getFilteredTasks(query, statusIndex, priorityIndex)`.
> 3. Отрефактори `ui.MainFrame`. Окно больше не должно выполнять логику само. По клику на "Удалить" или "Поиск" оно должно вызывать соответствующие методы `TaskController`.
> 4. Отрефактори `Main.java`, чтобы он правильно связывал эту архитектуру: инициализировал Model (`TaskManager`), Controller (`TaskController`) и View (`MainFrame`), связывая их через Dependency Injection.
> 
> *Примечание:* `TaskTableModel` оставь в пакете `ui`, так как это адаптер Swing-таблицы (часть View).

**Результат этапа:** Внедрен пакет `controller` с классом `TaskController`. `MainFrame` делегирует операции контроллеру.

---

## Этап 5: Валидация данных, обработка ошибок и диалоговые окна (JOptionPane)
**Автор промпта:** Студент Б

**Цель:** Предотвращение некорректных действий пользователя, добавление строгой валидации ввода (пустые заголовки, некорректный формат дат), внедрение диалоговых окон с предупреждениями и ошибками через `JOptionPane`, глобальная обработка непредвиденных исключений.

**Промпт:**
> Приложение не должно падать при неправильных действиях пользователя. добавь проверки (например: нельзя создать задачу без названия; нельзя ввести некорректную дату). При ошибке должно выскакивать понятное диалоговое окно с предупреждением (через JOptionPane).
> Внедрите проверки и окна с ошибками.\/C:/Users/USER/Desktop/QA1/untitled/src/Main.java

**Результат этапа:**
1. В `TaskDialog` внедрена валидация пустого названия задачи и парсинга дедлайна по формату `дд.ММ.гггг ЧЧ:мм` с показом окон `JOptionPane.WARNING_MESSAGE` и `JOptionPane.ERROR_MESSAGE`.
2. В `MainFrame` добавлены проверки на выбор строки перед редактированием или удалением, окно подтверждения удаления (`JOptionPane.showConfirmDialog`) и обработка ошибок ввода-вывода при загрузке/сохранении.
3. В `Main.java` добавлен глобальный перехватчик исключений `Thread.setDefaultUncaughtExceptionHandler`, предотвращающий падение приложения.
4. В `Task.java` и `FileManager.java` добавлены проверки на null и защита от поврежденных строк.

---

## Этап 6: Модернизация пользовательского интерфейса (Modern UI Styling)
**Автор промпта:** Студент Б

**Цель:** Стилизация графического интерфейса: улучшение типографики и включение сглаживания шрифтов, цветовая индикация приоритетов и статусов задач, настройка отступов (padding/margin), современный макет формы и статус-бар.

**Промпт:**
> сделать дизайн более современным: изменить шрифты, добавить цвета (например, красный фон для высокого приоритета), настроить красивые отступы (padding/margin) в Swing.
> Внесите финальные правки в UI.\/C:/Users/USER/Desktop/QA1/untitled/src/Main.java

**Результат этапа:**
1. В `Main.java` активировано аппаратное сглаживание шрифтов (`awt.useSystemAAFontSettings`, `swing.aatext`) и установлен системный шрифт `Segoe UI` 13pt.
2. В `MainFrame` реализован кастомный рендерер ячеек `TaskTableCellRenderer` с цветовой подсветкой приоритетов (Высокий — мягкий красный, Средний — желтый, Низкий — зеленый) и статусов.
3. Увеличена высота строк таблицы до 34px, настроено чередование фона (зебра) и редактирование по двойному клику.
4. Оформлены панели с отступами `EmptyBorder`/`Insets`, кнопки с иконками, и добавлен нижний статус-бар со статистикой задач.
5. `TaskDialog` переведен на сеточный макет `GridBagLayout` с акцентными кнопками и интуитивными отступами.
