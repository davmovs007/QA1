# Журнал промптов (Prompt Engineering Log)

В данном файле задокументированы все запросы (промпты), которые отправлялись искусственному интеллекту на каждом этапе разработки проекта. 

---

## Этап 1: Создание предметной области и бизнес-логики (Model & Service)
**Автор промпта:** Студент Б

**Промпт:**
> **Роль:** Ты Senior Java Backend Developer.
> 
> **Стек и ограничения:** 
> - Только чистая Java (стандартные библиотеки). 
> - Никаких систем сборки (Maven, Gradle).
> - Без юнит-тестов.
> 
> **Задание:**
> Напиши базовую архитектуру для приложения "Менеджер Задач" (To-Do List). 
> 1. Создай пакет `model`. В нем сделай класс `Task` (id, title, description, createdAt, dueDate). Также добавь два enum: `Priority` (LOW, MEDIUM, HIGH) и `TaskStatus` (TODO, IN_PROGRESS, DONE). Задай им русские названия через метод `getDisplayName()`.
> 2. Создай пакет `service`. В нем напиши класс `TaskManager`, который будет инкапсулировать список задач и отвечать за CRUD-операции над ними.
> 3. Добавь метод для сохранения и загрузки задач в CSV файл с использованием `java.io`.

---

## Этап 2: Создание базового графического интерфейса
**Автор промпта:** Студент А

**Промпт:**
> **Роль:** Ты Senior Java Developer. 
> 
> **Стек:** Только Java и Java Swing. Использование сборок и сторонних либ запрещено.
> 
> **Контекст задачи:** 
> Мой напарник уже разработал бизнес-логику для Менеджера Задач (`model` и `service`).
> 
> **Задание:**
> Напиши визуальную часть приложения (пакет `ui`). 
> 1. `MainFrame` — Главное окно, содержащее `JTable` для отображения задач и панель кнопок.
> 2. `TaskTableModel` — Собственная модель таблицы (`AbstractTableModel`), связывающая список с JTable.
> 3. `TaskDialog` — Окно для создания/редактирования задач.
> 4. `Main` — Главный класс, вызывающий `MainFrame` через `SwingUtilities.invokeLater()`.

---

## Этап 3: Добавление поиска и фильтрации в интерфейс
**Автор промпта:** Студент А

**Промпт:**
> **Задание:**
> Обнови интерфейс `MainFrame` и добавь панель фильтров:
> 1. Текстовое поле для поиска по названию.
> 2. Выпадающие списки (JComboBox) для фильтрации по `TaskStatus` и `Priority`.
> 3. Кнопку "Применить".
> 4. Метод, который использует `Stream API` для применения всех фильтров к списку `getAllTasks()` и передает результат в таблицу. Классы предметной области не меняй.

---

## Этап 4: Рефакторинг и MVC
**Автор промпта:** Студент А

**Промпт:**
> **Задание:**
> Отрефактори архитектуру под паттерн **MVC (Model-View-Controller)**. 
> Создай пакет `controller` и класс `TaskController`. Перенеси всю бизнес-логику работы с фильтрами, кнопками и файлами из окна `MainFrame` в `TaskController`. Окно View должно быть "глупым" и только вызывать методы Контроллера. В классе `Main` настрой правильную сборку (построй Model -> Controller -> View).

---

## Этап 5: UI/UX и Кроссплатформенность шрифтов (Исправление кракозябр)
**Автор промпта:** Студент А

**Промпт:**
> **Задание:**
> В приложении есть проблема с отображением текста в Windows (появляются квадратики и символы вместо русских букв). Кроме того, я хочу, чтобы весь текст был строго черного цвета. 
> 1. Модифицируй `Main.java`. Исправь метод установки шрифтов так, чтобы мы использовали умный системный шрифт Java `"SansSerif"`. Этот шрифт автоматически маппит буквы в стандартный `Segoe UI` на Windows, исправляя проблему нечитаемого текста, но при этом умеет рисовать иконки. 
> 2. В `MainFrame` удали все цветовые стили текста. В `Main` напиши перебор `UIManager.put` и назначь ключу `...foreground` строго цвет `Color.BLACK`, чтобы весь текст (кнопки, таблицы) по всему приложению стал чёрным.
